#include "ui_state_lifecycle.h"

// Intentionally empty on first pass.
// We wire the calls first, then add behavior one state at a time.

void uiStateOnEnter(UIState)
{
}

void uiStateOnExit(UIState)
{
}